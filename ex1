thread = Thread.new do
  sleep(2) # Симуляція тривалого обчислення
  42 # Значення, яке повертає потік
end

puts "Очікуємо завершення потоку..."
result = thread.value # Чекає завершення потоку та отримує значення
puts "Результат: #{result}" # => Результат: 42

